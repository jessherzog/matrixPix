$("m00").onchange = function(){

  var a = $('m00').value;  
  var b = $('m01').value;  
  var c = $('m02').value;
  var d = $('m10').value;  
  var e = $('m11').value;  
  var f = $('m12').value;
  var g = $('m20').value;  
  var h = $('m21').value;  
  var i = $('m22').value;

  var customMatrix = [{
  name: 'custom',
  data:
   [[ Number(a),  Number(b),  Number(c)],
    [ Number(d),  Number(e),  Number(f)],
    [ Number(g),  Number(h),  Number(i)]]
  }];

  customMatrix.forEach(function(m) {

  var m = customMatrix[0];

  transformador0.reset();
  transformador0.convolve(m.data, m.divisor, m.offset);

  $('fn').innerHTML = m.data[0] + '<br>' + m.data[1] + '<br>' + m.data[2];

  });
};

$("m01").onchange = function(){

  var a = $('m00').value;  
  var b = $('m01').value;  
  var c = $('m02').value;
  var d = $('m10').value;  
  var e = $('m11').value;  
  var f = $('m12').value;
  var g = $('m20').value;  
  var h = $('m21').value;  
  var i = $('m22').value;

  var customMatrix = [{
  name: 'custom',
  data:
   [[ Number(a),  Number(b),  Number(c)],
    [ Number(d),  Number(e),  Number(f)],
    [ Number(g),  Number(h),  Number(i)]]
  }];

  customMatrix.forEach(function(m) {

  var m = customMatrix[0];

  transformador0.reset();
  transformador0.convolve(m.data, m.divisor, m.offset);

  $('fn').innerHTML = m.data[0] + '<br>' + m.data[1] + '<br>' + m.data[2];

  });
};

$("m02").onchange = function(){

  var a = $('m00').value;  
  var b = $('m01').value;  
  var c = $('m02').value;
  var d = $('m10').value;  
  var e = $('m11').value;  
  var f = $('m12').value;
  var g = $('m20').value;  
  var h = $('m21').value;  
  var i = $('m22').value;

  var customMatrix = [{
  name: 'custom',
  data:
   [[ Number(a),  Number(b),  Number(c)],
    [ Number(d),  Number(e),  Number(f)],
    [ Number(g),  Number(h),  Number(i)]]
  }];

  customMatrix.forEach(function(m) {

  var m = customMatrix[0];

  transformador0.reset();
  transformador0.convolve(m.data, m.divisor, m.offset);

  $('fn').innerHTML = m.data[0] + '<br>' + m.data[1] + '<br>' + m.data[2];

  });
};

$("m10").onchange = function(){

  var a = $('m00').value;  
  var b = $('m01').value;  
  var c = $('m02').value;
  var d = $('m10').value;  
  var e = $('m11').value;  
  var f = $('m12').value;
  var g = $('m20').value;  
  var h = $('m21').value;  
  var i = $('m22').value;

  var customMatrix = [{
  name: 'custom',
  data:
   [[ Number(a),  Number(b),  Number(c)],
    [ Number(d),  Number(e),  Number(f)],
    [ Number(g),  Number(h),  Number(i)]]
  }];

  customMatrix.forEach(function(m) {

  var m = customMatrix[0];

  transformador0.reset();
  transformador0.convolve(m.data, m.divisor, m.offset);

  $('fn').innerHTML = m.data[0] + '<br>' + m.data[1] + '<br>' + m.data[2];

  });
};

$("m11").onchange = function(){

  var a = $('m00').value;  
  var b = $('m01').value;  
  var c = $('m02').value;
  var d = $('m10').value;  
  var e = $('m11').value;  
  var f = $('m12').value;
  var g = $('m20').value;  
  var h = $('m21').value;  
  var i = $('m22').value;

  var customMatrix = [{
  name: 'custom',
  data:
   [[ Number(a),  Number(b),  Number(c)],
    [ Number(d),  Number(e),  Number(f)],
    [ Number(g),  Number(h),  Number(i)]]
  }];

  customMatrix.forEach(function(m) {

  var m = customMatrix[0];

  transformador0.reset();
  transformador0.convolve(m.data, m.divisor, m.offset);

  $('fn').innerHTML = m.data[0] + '<br>' + m.data[1] + '<br>' + m.data[2];

  });
};

$("m12").onchange = function(){

  var a = $('m00').value;  
  var b = $('m01').value;  
  var c = $('m02').value;
  var d = $('m10').value;  
  var e = $('m11').value;  
  var f = $('m12').value;
  var g = $('m20').value;  
  var h = $('m21').value;  
  var i = $('m22').value;

  var customMatrix = [{
  name: 'custom',
  data:
   [[ Number(a),  Number(b),  Number(c)],
    [ Number(d),  Number(e),  Number(f)],
    [ Number(g),  Number(h),  Number(i)]]
  }];

  customMatrix.forEach(function(m) {

  var m = customMatrix[0];

  transformador0.reset();
  transformador0.convolve(m.data, m.divisor, m.offset);

  $('fn').innerHTML = m.data[0] + '<br>' + m.data[1] + '<br>' + m.data[2];

  });
};

$("m20").onchange = function(){

  var a = $('m00').value;  
  var b = $('m01').value;  
  var c = $('m02').value;
  var d = $('m10').value;  
  var e = $('m11').value;  
  var f = $('m12').value;
  var g = $('m20').value;  
  var h = $('m21').value;  
  var i = $('m22').value;

  var customMatrix = [{
  name: 'custom',
  data:
   [[ Number(a),  Number(b),  Number(c)],
    [ Number(d),  Number(e),  Number(f)],
    [ Number(g),  Number(h),  Number(i)]]
  }];

  customMatrix.forEach(function(m) {

  var m = customMatrix[0];

  transformador0.reset();
  transformador0.convolve(m.data, m.divisor, m.offset);

  $('fn').innerHTML = m.data[0] + '<br>' + m.data[1] + '<br>' + m.data[2];

  });
};

$("m21").onchange = function(){

  var a = $('m00').value;  
  var b = $('m01').value;  
  var c = $('m02').value;
  var d = $('m10').value;  
  var e = $('m11').value;  
  var f = $('m12').value;
  var g = $('m20').value;  
  var h = $('m21').value;  
  var i = $('m22').value;

  var customMatrix = [{
  name: 'custom',
  data:
   [[ Number(a),  Number(b),  Number(c)],
    [ Number(d),  Number(e),  Number(f)],
    [ Number(g),  Number(h),  Number(i)]]
  }];

  customMatrix.forEach(function(m) {

  var m = customMatrix[0];

  transformador0.reset();
  transformador0.convolve(m.data, m.divisor, m.offset);

  $('fn').innerHTML = m.data[0] + '<br>' + m.data[1] + '<br>' + m.data[2];

  });
};

$("m22").onchange = function(){

  var a = $('m00').value;  
  var b = $('m01').value;  
  var c = $('m02').value;
  var d = $('m10').value;  
  var e = $('m11').value;  
  var f = $('m12').value;
  var g = $('m20').value;  
  var h = $('m21').value;  
  var i = $('m22').value;

  var customMatrix = [{
  name: 'custom',
  data:
   [[ Number(a),  Number(b),  Number(c)],
    [ Number(d),  Number(e),  Number(f)],
    [ Number(g),  Number(h),  Number(i)]]
  }];

  customMatrix.forEach(function(m) {

  var m = customMatrix[0];

  transformador0.reset();
  transformador0.convolve(m.data, m.divisor, m.offset);

  $('fn').innerHTML = m.data[0] + '<br>' + m.data[1] + '<br>' + m.data[2];

  });
};